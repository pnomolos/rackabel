# Fully specified so `pack --dry-run` echoes no inference notes (deterministic
# transcript). --dry-run mutates nothing and needs no node.
[extension]
name = "Clip Renamer"
author = "Jane Doe"
version = "0.1.0"
entry = "src/extension.ts"
minimum_api_version = "1.0.0"
